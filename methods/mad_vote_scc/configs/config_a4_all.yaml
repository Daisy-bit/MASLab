# Cluster A — variant A4: mad_vote + all three SCC modules (full overlay).
# Spectral/answer-plurality Triggering + contribution-guided top-k DAG Routing
# + contribution-weighted Aggregation. The headline "SCC-on-mad_vote" cell.

agents_num: 5
rounds_num: 3
task_type: auto

xverify_model_name: xverify-9b-c
xverify_max_tokens: 64
xverify_temperature: 0.0

enable_triggering: true
enable_routing: true
enable_weighted_aggregation: true

top_k: 2
variance_consensus_thr: 0.05
answer_consensus_min: 3
emb_model: model/all-MiniLM-L6-v2
